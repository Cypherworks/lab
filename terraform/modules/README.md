# Terraform modules

Reusable, parameterised Terraform modules. Each is a generic mechanism consumed
by the private deployment repo, which supplies all site-specific values. No real
addresses, account IDs, or secrets belong here.
