---
# Restart re-reads config and (once initialised) auto-unseals via AWS KMS — no
# manual unseal needed.
- name: Restart openbao
  become: true
  ansible.builtin.systemd_service:
    name: openbao
    state: restarted
