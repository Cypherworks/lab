{# Rendered to {{ authentik_data_dir }}/.env (0600). docker compose reads it for
   variable interpolation and the services load it via env_file. #}
PG_USER={{ authentik_pg_user }}
PG_DB={{ authentik_pg_db }}
PG_PASS={{ authentik_pg_password }}
AUTHENTIK_SECRET_KEY={{ authentik_secret_key }}
AUTHENTIK_IMAGE={{ authentik_image }}
AUTHENTIK_TAG={{ authentik_tag }}
COMPOSE_PORT_HTTP={{ authentik_http_port }}
COMPOSE_PORT_HTTPS={{ authentik_https_port }}
AUTHENTIK_ERROR_REPORTING__ENABLED=false
