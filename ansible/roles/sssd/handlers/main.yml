---
# SSSD caches aggressively; restart (not reload) to pick up config/CA changes, and
# clear the cache so stale identity data doesn't linger.
- name: Restart sssd
  become: true
  ansible.builtin.systemd_service:
    name: sssd
    state: restarted
