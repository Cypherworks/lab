{# Managed by Ansible (unbound role). Recursive validating resolver on loopback. #}
server:
    interface: {{ unbound_listen }}@{{ unbound_port }}
    do-ip4: yes
    do-ip6: no
    do-udp: yes
    do-tcp: yes

    # Loopback only — the local DNS frontend forwards here.
    access-control: 0.0.0.0/0 refuse
    access-control: 127.0.0.0/8 allow

    hide-identity: yes
    hide-version: yes
    harden-glue: yes
    harden-dnssec-stripped: yes
    harden-referral-path: yes
    qname-minimisation: yes
    aggressive-nsec: yes

    prefetch: yes
    prefetch-key: yes
    cache-min-ttl: {{ unbound_cache_min_ttl }}
    cache-max-ttl: {{ unbound_cache_max_ttl }}
    rrset-roundrobin: yes
    minimal-responses: yes
    edns-buffer-size: {{ unbound_edns_buffer_size }}

    # DNSSEC: the unbound package owns the root trust anchor via its own
    # root-auto-trust-anchor-file.conf (it seeds and RFC 5011-maintains
    # /var/lib/unbound/root.key). Declaring auto-trust-anchor-file again here
    # makes unbound load the anchor twice — "trust anchor presented twice" fatal
    # — so we don't. We only supply fresher root hints (from dns-root-data).
    root-hints: "/usr/share/dns/root.hints"
