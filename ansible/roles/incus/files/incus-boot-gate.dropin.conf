# Hold the Incus daemon until the cold-boot recovery gate clears (#188). Wants, not
# Requires, so a broken or missing gate never blocks the daemon from starting — the gate
# is best-effort and fail-open by design; the daemon must always come up unattended.
[Unit]
Wants=incus-boot-gate.service
After=incus-boot-gate.service
